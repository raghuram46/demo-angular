import { Customd1Directive } from './customd1.directive';

describe('Customd1Directive', () => {
  it('should create an instance', () => {
    const directive = new Customd1Directive();
    expect(directive).toBeTruthy();
  });
});
